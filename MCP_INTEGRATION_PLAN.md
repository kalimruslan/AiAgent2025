# План интеграции модуля MCP в Conversation

## 📋 Обзор

Цель: Интегрировать новый feature-модуль `features/mcp` в существующий модуль `features/conversation` для обеспечения управления MCP инструментами и их выполнением в рамках диалога с AI.

## 🏗️ Текущее состояние

### Модуль MCP (`features/mcp`)
✅ Создана структура модуля
✅ Реализован `McpViewModel` для управления состоянием
✅ Определён `McpState` с полями:
  - `availableTools` - список доступных инструментов
  - `isEnabled` - флаг включения MCP
  - `currentExecution` - текущее выполняемое действие
  - `executionHistory` - история выполнений
  - `isPanelExpanded` - состояние UI панели

✅ Созданы UI компоненты:
  - `McpToolsPanel` - основная панель управления
  - `McpToolCard` - карточка инструмента
  - `McpToolExecutionStatusIndicator` - индикатор выполнения

✅ Настроена DI с Koin (`mcpModule`)

### Модуль Conversation (`features/conversation`)
✅ Есть `ConversationViewModel` с MCP логикой:
  - `sendMessageWithMcpTools()` - отправка с использованием инструментов
  - `extractToolNameFromMessage()` - парсинг ответов
  - Поле `isUsedMcpTools` в State
  - Поле `availableTools` в State
  - Поле `currentToolExecution` типа `ToolExecutionStatus`

⚠️ **Проблема**: В conversation есть своя реализация работы с MCP, которая дублирует функциональность нового модуля

## 🎯 Цели интеграции

1. **Устранить дублирование**: Перенести MCP-специфичную логику из ConversationViewModel в McpViewModel
2. **Разделить ответственность**: Conversation управляет диалогом, MCP управляет инструментами
3. **Создать чистый API**: McpViewModel предоставляет интерфейс для Conversation
4. **Интегрировать UI**: Встроить McpToolsPanel в ConversationScreen
5. **Синхронизировать состояние**: Обеспечить корректную связь между двумя ViewModels

## 📐 Архитектурные решения

### 1. Разделение ответственности

**ConversationViewModel отвечает за:**
- Управление сообщениями и историей диалога
- Взаимодействие с Use Cases (ChatWithMcpToolsUseCase)
- Управление режимами (Single/Committee)
- Token usage и summarization
- RAG интеграция

**McpViewModel отвечает за:**
- Загрузку списка доступных инструментов
- Состояние включения/выключения MCP
- Отображение статуса выполнения инструментов
- Историю выполнения инструментов
- UI состояние панели MCP

### 2. Коммуникация между ViewModels

Используем **SharedFlow** или **StateFlow** для передачи событий:

```kotlin
// В McpViewModel
val toolExecutionEvents: SharedFlow<McpToolExecutionEvent>

// В ConversationViewModel
init {
    viewModelScope.launch {
        mcpViewModel.toolExecutionEvents.collect { event ->
            handleMcpEvent(event)
        }
    }
}
```

### 3. Источник данных об инструментах

`GetMcpToolsUseCase` будет вызываться **только** из `McpViewModel`. Conversation будет получать информацию через state MCP модуля.

## 🔧 Этапы реализации

### Этап 1: Подготовка зависимостей ⏱️ 15 мин

**Задача**: Настроить зависимости между модулями

1. **Обновить `features/conversation/build.gradle.kts`**
   ```kotlin
   commonMain.dependencies {
       // ... существующие зависимости
       implementation(projects.features.mcp)
   }
   ```

2. **Зарегистрировать `mcpModule` в DI**
   - Открыть `llm-app/src/commonMain/kotlin/ru/llm/agent/common/app/InitKoin.kt`
   - Добавить импорт: `import ru.llm.agent.mcp.di.mcpModule`
   - Добавить модуль в список: `modules(... + mcpModule)`

3. **Проверить компиляцию**
   ```bash
   ./gradlew clean build
   ```

**Критерии готовности:**
- ✅ Модуль conversation видит классы из модуля mcp
- ✅ McpViewModel доступна через Koin
- ✅ Проект успешно компилируется

---

### Этап 2: Рефакторинг ConversationViewModel ⏱️ 30 мин

**Задача**: Удалить дублирующую MCP логику из ConversationViewModel

#### 2.1. Удалить MCP-специфичные поля из ConversationUIState.State

**Файл**: `features/conversation/src/.../ConversationUIState.kt`

**Удалить:**
```kotlin
val isUsedMcpTools: Boolean = false
val availableTools: List<McpToolInfo>
val currentToolExecution: ToolExecutionStatus? = null
```

**Причина**: Эти поля теперь управляются McpState

#### 2.2. Удалить вспомогательные функции парсинга

**Файл**: `ConversationViewModel.kt`

**Удалить функции:**
- `extractToolNameFromMessage(messageText: String): String` (строки 682-691)
- `extractToolResultFromMessage(messageText: String): String` (строки 693-702)
- `switchNeedMcpTools(useTools: Boolean)` (строки 219-228)

**Причина**: Эта логика будет в McpViewModel или Use Case

#### 2.3. Обновить использование MCP данных

**Изменить:** Места в коде, где используются `state.isUsedMcpTools` и `state.availableTools`

**Было:**
```kotlin
if(_screeState.value.isUsedMcpTools){
    sendMessageWithMcpTools(message)
}
```

**Станет:**
```kotlin
if(mcpViewModel.state.value.isEnabled){
    sendMessageWithMcpTools(message)
}
```

#### 2.4. Внедрить McpViewModel через конструктор

**Файл**: `ConversationViewModel.kt`

**Добавить параметр:**
```kotlin
class ConversationViewModel(
    // ... существующие параметры
    private val mcpViewModel: McpViewModel  // НОВОЕ
) : ViewModel()
```

**Обновить в Koin модуле** (`ConversationKoin.kt`):
```kotlin
viewModel {
    ConversationViewModel(
        // ... существующие зависимости
        mcpViewModel = get()
    )
}
```

**Критерии готовности:**
- ✅ Нет дублирующих полей в ConversationUIState
- ✅ Удалены функции парсинга из ConversationViewModel
- ✅ McpViewModel инжектится через Koin
- ✅ Код компилируется без ошибок

---

### Этап 3: Обновить логику sendMessageWithMcpTools ⏱️ 20 мин

**Задача**: Адаптировать метод для работы с новым McpViewModel

**Файл**: `ConversationViewModel.kt`, функция `sendMessageWithMcpTools()` (строки 253-309)

#### 3.1. Использовать данные из McpViewModel

**Заменить:**
```kotlin
availableTools = _screeState.value.availableTools
```

**На:**
```kotlin
availableTools = mcpViewModel.state.value.availableTools
```

#### 3.2. Обновить статус выполнения инструмента

**Текущий код** (строки 268-285):
```kotlin
if (conversationMessage.isContinue) {
    val toolName = extractToolNameFromMessage(conversationMessage.text)
    val result = extractToolResultFromMessage(conversationMessage.text)

    _screeState.update { state ->
        state.copy(
            currentToolExecution = ConversationUIState.ToolExecutionStatus(...)
        )
    }
}
```

**Изменить на:**
```kotlin
if (conversationMessage.isContinue) {
    // Парсинг можно вынести в extension или utility
    val toolName = conversationMessage.text.extractToolName()
    val result = conversationMessage.text.extractToolResult()

    // Обновляем состояние через McpViewModel
    mcpViewModel.updateToolExecution(
        toolName = toolName,
        description = result,
        isExecuting = true
    )

    _screeState.update { state ->
        state.copy(
            isLoading = false,
            isConversationComplete = false,
            requestTokens = null
        )
    }
}
```

#### 3.3. Очистить статус при завершении

**Текущий код** (строки 287-296):
```kotlin
else {
    _screeState.update { state ->
        state.copy(
            currentToolExecution = null
        )
    }
}
```

**Изменить на:**
```kotlin
else {
    // Уведомляем MCP ViewModel о завершении
    mcpViewModel.clearCurrentExecution()

    _screeState.update { state ->
        state.copy(
            isLoading = false,
            isConversationComplete = false,
            requestTokens = null
        )
    }
}
```

**Критерии готовности:**
- ✅ sendMessageWithMcpTools использует данные из McpViewModel
- ✅ Статус выполнения обновляется через McpViewModel
- ✅ Нет прямого обращения к удалённым полям State

---

### Этап 4: Расширить API McpViewModel ⏱️ 25 мин

**Задача**: Добавить методы для взаимодействия с ConversationViewModel

**Файл**: `features/mcp/src/.../McpViewModel.kt`

#### 4.1. Добавить методы управления выполнением

```kotlin
/**
 * Обновить статус выполнения инструмента
 */
fun updateToolExecution(
    toolName: String,
    description: String,
    isExecuting: Boolean,
    result: String? = null
) {
    val execution = McpToolExecutionStatus(
        toolName = toolName,
        description = description,
        isExecuting = isExecuting,
        result = result
    )

    _state.update {
        it.copy(currentExecution = execution)
    }
}

/**
 * Очистить текущее выполнение и добавить в историю
 */
fun clearCurrentExecution() {
    val current = _state.value.currentExecution
    if (current != null) {
        _state.update {
            it.copy(
                currentExecution = null,
                executionHistory = it.executionHistory + current
            )
        }
    }
}

/**
 * Получить список доступных инструментов (для использования в других модулях)
 */
fun getAvailableTools(): List<McpToolInfo> {
    return _state.value.availableTools
}

/**
 * Проверить, включены ли MCP инструменты
 */
fun isMcpEnabled(): Boolean {
    return _state.value.isEnabled
}
```

#### 4.2. Добавить событие для интеграции с ChatWithMcpToolsUseCase

```kotlin
sealed interface McpEvent {
    // ... существующие события

    /**
     * Обновить статус выполнения извне (из Conversation)
     */
    data class UpdateExternalExecution(
        val toolName: String,
        val description: String,
        val isExecuting: Boolean,
        val result: String? = null
    ) : McpEvent
}
```

```kotlin
private fun handleEvent(event: McpEvent) {
    when (event) {
        // ... существующие
        is McpEvent.UpdateExternalExecution -> {
            updateToolExecution(
                toolName = event.toolName,
                description = event.description,
                isExecuting = event.isExecuting,
                result = event.result
            )
        }
    }
}
```

**Критерии готовности:**
- ✅ Добавлены публичные методы для управления состоянием
- ✅ Можно обновлять статус выполнения извне
- ✅ История выполнений корректно формируется

---

### Этап 5: Интеграция UI в ConversationScreen ⏱️ 30 мин

**Задача**: Встроить McpToolsPanel в экран диалога

**Файл**: `features/conversation/src/.../ConversationScreen.kt`

#### 5.1. Инжектировать McpViewModel

```kotlin
@Composable
fun ConversationScreen(
    viewModel: ConversationViewModel = koinViewModel(),
    mcpViewModel: McpViewModel = koinViewModel()  // НОВОЕ
) {
    val state by viewModel.screeState.collectAsState()
    val mcpState by mcpViewModel.state.collectAsState()  // НОВОЕ

    // ...
}
```

#### 5.2. Добавить McpToolsPanel в layout

**Вариант A: Панель сверху (над списком сообщений)**

```kotlin
Column(modifier = Modifier.fillMaxSize()) {
    // Toolbar с настройками
    ConversationToolbar(...)

    // MCP панель (сворачиваемая)
    if (mcpState.isEnabled || mcpState.availableTools.isNotEmpty()) {
        McpToolsPanel(
            viewModel = mcpViewModel,
            modifier = Modifier
                .fillMaxWidth()
                .padding(horizontal = 16.dp, vertical = 8.dp)
        )
    }

    // Список сообщений
    ConversationMessagesList(...)

    // Поле ввода
    ConversationInputField(...)
}
```

**Вариант B: Боковая панель (для Desktop)**

```kotlin
Row(modifier = Modifier.fillMaxSize()) {
    // Основной контент
    Column(modifier = Modifier.weight(1f)) {
        // Сообщения и ввод
    }

    // Боковая MCP панель
    if (mcpState.isEnabled) {
        McpToolsPanel(
            viewModel = mcpViewModel,
            modifier = Modifier
                .width(300.dp)
                .fillMaxHeight()
        )
    }
}
```

#### 5.3. Показывать статус выполнения инструмента

```kotlin
// В области над полем ввода
mcpState.currentExecution?.let { execution ->
    McpToolExecutionStatusIndicator(
        status = execution,
        modifier = Modifier
            .fillMaxWidth()
            .padding(8.dp)
    )
}
```

#### 5.4. Добавить переключатель MCP в настройки/toolbar

```kotlin
// В ConversationToolbar или отдельном меню
Row(
    verticalAlignment = Alignment.CenterVertically,
    horizontalArrangement = Arrangement.spacedBy(8.dp)
) {
    Text("MCP Tools")
    Switch(
        checked = mcpState.isEnabled,
        onCheckedChange = { enabled ->
            mcpViewModel.onEvent(McpEvent.ToggleEnabled(enabled))
        }
    )
}
```

**Критерии готовности:**
- ✅ McpToolsPanel отображается на ConversationScreen
- ✅ Панель реагирует на изменения состояния
- ✅ Статус выполнения инструмента виден пользователю
- ✅ Можно включить/выключить MCP через UI

---

### Этап 6: Синхронизация состояний ⏱️ 20 мин

**Задача**: Обеспечить корректное взаимодействие между ViewModels

**Файл**: `ConversationViewModel.kt`

#### 6.1. Подписаться на изменения состояния MCP

```kotlin
init {
    // ... существующий код

    // Подписка на события MCP
    viewModelScope.launch {
        mcpViewModel.state.collect { mcpState ->
            handleMcpStateChange(mcpState)
        }
    }
}

private fun handleMcpStateChange(mcpState: McpState) {
    // Логируем изменения для отладки
    if (mcpState.currentExecution != null) {
        Logger.getLogger("MCP-Integration")
            .info("Tool execution: ${mcpState.currentExecution.toolName}")
    }

    // Можно добавить дополнительную логику реакции на изменения MCP
}
```

#### 6.2. Обновить логику включения/выключения MCP в sendMessageToAi

```kotlin
private fun sendMessageToAi(message: String) {
    if (message.isBlank() || _screeState.value.isLoading) return

    when (_screeState.value.selectedMode) {
        ConversationMode.SINGLE -> {
            // Проверяем через McpViewModel
            if(mcpViewModel.isMcpEnabled()){
                sendMessageWithMcpTools(message)
            } else{
                sendMessageToSingleAi(message)
            }
        }
        ConversationMode.COMMITTEE -> sendMessageToCommittee(message)
    }
}
```

**Критерии готовности:**
- ✅ ConversationViewModel реагирует на изменения McpState
- ✅ Включение/выключение MCP корректно обрабатывается
- ✅ Нет race conditions между ViewModels

---

### Этап 7: Создать утилиты для парсинга ⏱️ 15 мин

**Задача**: Вынести парсинг tool execution в переиспользуемые функции

**Новый файл**: `features/mcp/src/.../utils/McpMessageParser.kt`

```kotlin
package ru.llm.agent.mcp.utils

/**
 * Извлечь название инструмента из текста сообщения
 * Формат: "Выполнение инструмента: tool_name\nРезультат: ..."
 */
fun String.extractToolName(): String {
    return try {
        val pattern = "Выполнение инструмента: ([^\\n]+)".toRegex()
        val match = pattern.find(this)
        match?.groupValues?.getOrNull(1)?.trim() ?: "Инструмент"
    } catch (e: Exception) {
        "Инструмент"
    }
}

/**
 * Извлечь результат выполнения из текста сообщения
 * Формат: "Результат: result_text"
 */
fun String.extractToolResult(): String {
    return try {
        val pattern = "Результат: ([^\\n]+)".toRegex()
        val match = pattern.find(this)
        match?.groupValues?.getOrNull(1)?.trim() ?: ""
    } catch (e: Exception) {
        ""
    }
}

/**
 * Проверить, является ли сообщение результатом выполнения инструмента
 */
fun String.isToolExecutionMessage(): Boolean {
    return contains("Выполнение инструмента:")
}
```

#### Использовать в ConversationViewModel:

```kotlin
import ru.llm.agent.mcp.utils.extractToolName
import ru.llm.agent.mcp.utils.extractToolResult

// В sendMessageWithMcpTools:
if (conversationMessage.isContinue) {
    val toolName = conversationMessage.text.extractToolName()
    val result = conversationMessage.text.extractToolResult()
    // ...
}
```

**Критерии готовности:**
- ✅ Создан файл с extension функциями
- ✅ ConversationViewModel использует новые утилиты
- ✅ Парсинг работает корректно

---

### Этап 8: Обновить тесты ⏱️ 30 мин

**Задача**: Адаптировать существующие тесты и добавить новые

#### 8.1. Обновить тесты ConversationViewModel

**Файл**: `features/conversation/src/commonTest/.../ConversationViewModelTest.kt`

- Mock `McpViewModel` в конструкторе
- Проверить корректность вызовов `isMcpEnabled()` и `getAvailableTools()`
- Добавить тест на взаимодействие между ViewModels

#### 8.2. Создать тесты для McpViewModel

**Новый файл**: `features/mcp/src/commonTest/.../McpViewModelTest.kt`

Покрыть тестами:
- Загрузку инструментов
- Включение/выключение MCP
- Обновление статуса выполнения
- Формирование истории

#### 8.3. Тесты утилит парсинга

**Новый файл**: `features/mcp/src/commonTest/.../McpMessageParserTest.kt`

```kotlin
@Test
fun `extractToolName should parse tool name correctly`() {
    val message = "Выполнение инструмента: trello_get_cards\nРезультат: ..."
    assertEquals("trello_get_cards", message.extractToolName())
}

@Test
fun `extractToolResult should parse result correctly`() {
    val message = "Результат: Success"
    assertEquals("Success", message.extractToolResult())
}
```

**Критерии готовности:**
- ✅ Все существующие тесты проходят
- ✅ Добавлены новые тесты для McpViewModel
- ✅ Покрытие парсинга утилит > 80%

---

### Этап 9: Финальное тестирование и отладка ⏱️ 40 мин

**Задача**: Проверить интеграцию end-to-end

#### 9.1. Ручное тестирование на Desktop

**Сценарий 1: Включение MCP**
1. Запустить приложение
2. Открыть ConversationScreen
3. Включить переключатель MCP
4. Убедиться, что панель инструментов отображается
5. Проверить, что список инструментов загружается

**Сценарий 2: Отправка сообщения с MCP**
1. Включить MCP
2. Отправить сообщение, требующее использования инструмента
3. Убедиться, что статус выполнения отображается
4. Проверить, что результат выполнения виден в истории

**Сценарий 3: Выключение MCP**
1. Выключить переключатель MCP
2. Отправить сообщение
3. Убедиться, что используется обычный режим (без инструментов)

#### 9.2. Проверка на Android

- Запустить на эмуляторе Android
- Повторить сценарии из 9.1
- Убедиться в корректности UI на мобильной платформе

#### 9.3. Логирование и отладка

Добавить подробное логирование:

```kotlin
// В ConversationViewModel
Logger.getLogger("MCP-Integration").info(
    "MCP enabled: ${mcpViewModel.isMcpEnabled()}, " +
    "Available tools: ${mcpViewModel.getAvailableTools().size}"
)

// В McpViewModel
Logger.getLogger("MCP-Tools").info(
    "Tool execution updated: ${execution.toolName}, " +
    "Status: ${if (execution.isExecuting) "running" else "completed"}"
)
```

#### 9.4. Проверить производительность

- Убедиться, что нет утечек памяти при переключении MCP
- Проверить, что подписки на Flow корректно отменяются
- Измерить задержку при отправке сообщений с MCP

**Критерии готовности:**
- ✅ Все сценарии работают на Desktop и Android
- ✅ Нет критических багов
- ✅ UI responsive и без лагов
- ✅ Логи показывают корректную работу интеграции

---

### Этап 10: Документация и cleanup ⏱️ 20 мин

**Задача**: Обновить документацию и убрать временный код

#### 10.1. Обновить CLAUDE.md

Добавить раздел о MCP интеграции:

```markdown
### MCP Интеграция

Модуль `features/mcp` предоставляет управление MCP (Model Context Protocol) инструментами.

**Архитектура:**
- `McpViewModel` управляет состоянием инструментов и их выполнением
- `ConversationViewModel` использует MCP для расширенных возможностей AI
- `McpToolsPanel` - UI компонент для отображения и управления инструментами

**Использование:**
```kotlin
// В Compose
val mcpViewModel: McpViewModel = koinViewModel()
McpToolsPanel(viewModel = mcpViewModel)

// Проверка включения MCP
if (mcpViewModel.isMcpEnabled()) {
    // Логика с использованием инструментов
}
```

**Добавление нового инструмента:**
1. Определить инструмент в MCP сервере (`server/`)
2. McpViewModel автоматически загрузит его через `GetMcpToolsUseCase`
3. Инструмент появится в UI панели
```
```

#### 10.2. Удалить неиспользуемый код

Проверить и удалить:
- Закомментированные функции (`startBoardMonitoring`, `stopBoardMonitoring`)
- Временные TODO комментарии
- Дублирующие импорты

#### 10.3. Создать миграционный гайд (если нужно)

Если другие разработчики используют проект:

**Файл**: `MIGRATION_MCP.md`

```markdown
# Миграция на новый MCP модуль

Если вы использовали старую MCP интеграцию в ConversationViewModel:

## Что изменилось

- `ConversationUIState.isUsedMcpTools` → `McpState.isEnabled`
- `ConversationUIState.availableTools` → `McpState.availableTools`
- `ConversationUIState.currentToolExecution` → `McpState.currentExecution`

## Как обновить код

### Было:
```kotlin
val state = conversationViewModel.state.value
if (state.isUsedMcpTools) {
    // ...
}
```

### Стало:
```kotlin
val mcpState = mcpViewModel.state.value
if (mcpState.isEnabled) {
    // ...
}
```
```

**Критерии готовности:**
- ✅ CLAUDE.md обновлён
- ✅ Удалён весь временный и закомментированный код
- ✅ Создан миграционный гайд (если применимо)

---

## 📊 Чек-лист перед завершением

### Функциональность
- [ ] MCP панель отображается на ConversationScreen
- [ ] Можно включить/выключить MCP через UI
- [ ] Список инструментов загружается корректно
- [ ] Статус выполнения инструмента отображается в реальном времени
- [ ] История выполнений сохраняется и отображается
- [ ] Отправка сообщений с MCP работает корректно
- [ ] Отправка сообщений без MCP работает корректно

### Архитектура
- [ ] Нет дублирования логики между модулями
- [ ] Зависимости настроены корректно
- [ ] DI (Koin) работает без ошибок
- [ ] Соблюдается Clean Architecture

### Код-качество
- [ ] Все тесты проходят
- [ ] Покрытие тестами > 70%
- [ ] Нет предупреждений компилятора
- [ ] Код соответствует style guide проекта

### Документация
- [ ] Обновлён CLAUDE.md
- [ ] Добавлены KDoc комментарии на русском
- [ ] Создан миграционный гайд (если нужно)

### Платформы
- [ ] Работает на Desktop (JVM)
- [ ] Работает на Android
- [ ] UI адаптирован под разные размеры экрана

---

## 🎓 Обучающие заметки

### Ключевые паттерны использованные в интеграции

1. **Feature Module Pattern**: Каждый feature-модуль инкапсулирует свою функциональность
2. **ViewModel Communication**: ViewModels общаются через StateFlow/SharedFlow
3. **Dependency Injection**: Koin обеспечивает слабую связанность
4. **Separation of Concerns**: Каждый модуль отвечает за свою domain область

### Возможные проблемы и решения

**Проблема**: Race condition при одновременном обновлении состояний
**Решение**: Использовать `Mutex` или `Channel` для синхронизации

**Проблема**: Утечка памяти из-за незакрытых подписок
**Решение**: Использовать `viewModelScope.launch` для автоматической отмены

**Проблема**: UI лагает при загрузке большого списка инструментов
**Решение**: Использовать `LazyColumn` и пагинацию

---

## 📈 Дальнейшие улучшения (Post-MVP)

1. **Фильтрация инструментов**: Поиск и категории в McpToolsPanel
2. **Избранные инструменты**: Пользователь может закрепить часто используемые
3. **Статистика использования**: Аналитика по вызовам инструментов
4. **Настройки инструментов**: Конфигурация параметров для каждого инструмента
5. **Импорт/экспорт конфигурации**: Сохранение настроек MCP
6. **Темизация**: Кастомные цвета для разных типов инструментов
7. **Уведомления**: Push-уведомления при завершении долгих операций

---

## ⏱️ Оценка времени

| Этап | Время | Приоритет |
|------|-------|-----------|
| 1. Подготовка зависимостей | 15 мин | Высокий |
| 2. Рефакторинг ConversationViewModel | 30 мин | Высокий |
| 3. Обновить sendMessageWithMcpTools | 20 мин | Высокий |
| 4. Расширить API McpViewModel | 25 мин | Высокий |
| 5. Интеграция UI | 30 мин | Высокий |
| 6. Синхронизация состояний | 20 мин | Средний |
| 7. Утилиты парсинга | 15 мин | Средний |
| 8. Тесты | 30 мин | Высокий |
| 9. Финальное тестирование | 40 мин | Высокий |
| 10. Документация | 20 мин | Средний |
| **ИТОГО** | **~4 часа** | |

---

## 🚀 Готовность к старту

**Перед началом реализации убедитесь:**

✅ Вы понимаете архитектуру обоих модулей
✅ У вас есть доступ ко всем необходимым файлам
✅ Проект успешно компилируется на текущий момент
✅ Вы сделали backup или коммит текущего состояния
✅ У вас настроена среда разработки (IDE, эмулятор)

**Начинайте с Этапа 1!** 🎯