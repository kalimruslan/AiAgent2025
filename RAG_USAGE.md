# 🚀 Инструкция по использованию RAG в приложении

## ✅ Что готово

### Backend
- ✅ `IndexTextUseCase` - индексация текста в базу знаний
- ✅ `AskWithRagUseCase` - запросы с использованием RAG
- ✅ `GetRagIndexStatsUseCase` - статистика индекса
- ✅ `ClearRagIndexUseCase` - очистка базы знаний
- ✅ Интеграция с Ollama для embeddings (модель `nomic-embed-text`)
- ✅ Векторное хранилище в памяти с косинусным расстоянием
- ✅ Разбиение текста на чанки (800 символов с overlap 200)

### UI
- ✅ `RagControlPanel` - панель управления RAG
- ✅ `KnowledgeBaseDialog` - диалог для добавления знаний
- ✅ Интеграция в `ConversationScreen`
- ✅ Автоматическое переключение между обычным и RAG режимом

### ViewModel
- ✅ События для управления RAG
- ✅ Обработчики для всех операций
- ✅ Автоматическая интеграция в поток сообщений

## 📖 Как использовать

### 1. Запуск Ollama Server

Убедись, что Ollama запущена и модель для embeddings доступна:

```bash
# Проверь что Ollama запущена
curl http://localhost:11434/api/tags

# Если модели нет, загрузи её
ollama pull nomic-embed-text
```

### 2. Запуск приложения

```bash
./gradlew :llm-app:run
```

### 3. Использование RAG в UI

1. **Включить RAG**
   - В нижней части экрана найди панель "RAG (База знаний)"
   - Переключи switch в положение ON

2. **Добавить знания**
   - Нажми кнопку "Добавить знания"
   - В диалоге:
     - Введи название источника (например: "kotlin-guide")
     - Вставь текст, который хочешь использовать для ответов
   - Нажми "Добавить"
   - Текст автоматически разобьется на чанки и проиндексируется

3. **Задать вопрос**
   - После добавления знаний просто пиши вопросы как обычно
   - RAG автоматически найдет релевантные фрагменты и добавит их в контекст
   - LLM получит вопрос + релевантные фрагменты из базы знаний

4. **Очистить базу знаний**
   - Нажми кнопку с иконкой корзины рядом с "Добавить знания"

## 🔧 Настройки RAG

В коде можно настроить параметры RAG:

### В `sendMessageToSingleAi()` (ConversationViewModel.kt:385-393)

```kotlin
askWithRagUseCase.invoke(
    conversationId = conversationId,
    userMessage = message,
    provider = _screeState.value.selectedProvider,
    topK = 3,        // Количество релевантных фрагментов (по умолчанию 3)
    threshold = 0.3  // Порог similarity (0.0-1.0, по умолчанию 0.3)
)
```

### В `TextChunker.kt`

```kotlin
private const val CHUNK_SIZE = 800        // Размер чанка в символах
private const val CHUNK_OVERLAP = 200     // Overlap между чанками
```

## 📊 Как работает RAG

1. **Индексация** (`IndexTextUseCase`)
   - Текст разбивается на чанки (800 символов с overlap 200)
   - Для каждого чанка генерируется embedding через Ollama
   - Чанки с embeddings сохраняются в векторном хранилище

2. **Поиск** (`AskWithRagUseCase`)
   - Генерируется embedding для вопроса пользователя
   - Ищутся топ-K наиболее похожих чанков (косинусное расстояние)
   - Фильтруются чанки с similarity < threshold
   - Релевантные чанки добавляются в промпт LLM

3. **Генерация ответа**
   - LLM получает: system prompt + релевантные фрагменты + история + вопрос
   - LLM генерирует ответ на основе предоставленных знаний

## 🧪 Пример использования

### Тестовый сценарий

1. Включи RAG
2. Добавь знания:
   ```
   Название: kotlin-kmp
   Текст: Kotlin Multiplatform позволяет делиться кодом между платформами.
   Для создания expect/actual используй ключевые слова expect и actual.
   expect fun platformFunction(): String
   actual fun platformFunction(): String = "Android"
   ```
3. Задай вопрос: "Как использовать expect/actual в Kotlin?"
4. LLM ответит используя знания из базы

## 🐛 Troubleshooting

### Ошибка "Connection refused"
- Проверь что Ollama запущена: `curl http://localhost:11434/api/tags`
- Проверь порт в `OllamaApi.kt` (должен быть 11434)

### Пустые ответы
- Проверь что модель `nomic-embed-text` установлена: `ollama list`
- Увеличь `threshold` (например до 0.5) если слишком строгая фильтрация
- Увеличь `topK` для большего контекста

### Медленная работа
- Уменьши `topK` (например до 2)
- Уменьши размер чанков в `TextChunker.kt`

## 📚 Архитектура

```
UI Layer (ConversationScreen)
    ↓
ViewModel (ConversationViewModel)
    ↓
Domain Layer (IndexTextUseCase, AskWithRagUseCase)
    ↓
Data Layer (RagRepositoryImpl)
    ↓
Services (EmbeddingService, TextChunker, VectorStore)
    ↓
Ollama API (embeddings)
```

## 🎯 Следующие шаги

Для улучшения RAG можно добавить:

1. **Персистентное хранилище** - сохранять индекс в БД
2. **Метаданные** - добавить фильтрацию по источнику/дате
3. **Hybrid search** - комбинировать semantic + keyword search
4. **Re-ranking** - улучшить качество выбора фрагментов
5. **Chunking strategies** - разные стратегии разбиения (по абзацам, предложениям)
6. **Feedback loop** - учитывать отзывы пользователей о релевантности

## 📝 Файлы для изучения

- `llm-domain/src/commonMain/kotlin/ru.llm.agent/usecase/rag/` - Use cases
- `llm-data/src/commonMain/kotlin/ru.llm.agent/service/` - Сервисы RAG
- `features/conversation/src/commonMain/kotlin/ru/llm/agent/presentation/ui/components/RagControlPanel.kt` - UI
- `features/conversation/src/commonMain/kotlin/ru/llm/agent/presentation/viewmodel/ConversationViewModel.kt:734-836` - Логика ViewModel