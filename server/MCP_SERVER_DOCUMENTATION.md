# MCP Server Documentation

## Обзор

Это реализация MCP (Model Context Protocol) сервера на базе Ktor, который предоставляет набор инструментов (tools) для взаимодействия с AI-агентами. Сервер поддерживает JSON-RPC протокол и предоставляет различные утилиты для расширения возможностей LLM-агентов.

## Архитектура

### Основные компоненты

- **KtorServer.kt** - Точка входа Ktor приложения, настройка HTTP сервера
- **McpServer.kt** - Основная логика обработки MCP запросов
- **JSON-RPC** - Протокол обмена сообщениями (запросы/ответы)
- **MCP Models** - Модели данных для инструментов и capabilities

### Технологический стек

- Kotlin 2.2.0
- Ktor 3.2.1 (Server + Client)
- Kotlinx Serialization для JSON
- JSON-RPC 2.0 протокол

## Запуск сервера

```bash
# Сборка проекта
./gradlew :server:build

# Запуск сервера
./gradlew :server:run

# Сборка shadow JAR
./gradlew :server:shadowJar
```

По умолчанию сервер запускается на порту `8080`.

## MCP Protocol Methods

Сервер поддерживает следующие методы JSON-RPC:

### 1. initialize

Инициализация соединения с сервером.

**Запрос:**
```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "method": "initialize",
  "params": {}
}
```

**Ответ:**
```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "result": {
    "serverInfo": {
      "name": "Simple MCP Server",
      "version": "1.0.0"
    },
    "capabilities": {
      "tools": {
        "listChanged": false
      }
    }
  }
}
```

### 2. tools/list

Получение списка доступных инструментов.

**Запрос:**
```json
{
  "jsonrpc": "2.0",
  "id": "2",
  "method": "tools/list",
  "params": {}
}
```

**Ответ:**
```json
{
  "jsonrpc": "2.0",
  "id": "2",
  "result": {
    "tools": [
      {
        "name": "echo",
        "description": "Возвращает введенный текст",
        "inputSchema": { ... }
      },
      ...
    ]
  }
}
```

### 3. tools/call

Вызов конкретного инструмента.

**Запрос:**
```json
{
  "jsonrpc": "2.0",
  "id": "3",
  "method": "tools/call",
  "params": {
    "name": "calculate",
    "arguments": {
      "expression": "2 + 2"
    }
  }
}
```

**Ответ:**
```json
{
  "jsonrpc": "2.0",
  "id": "3",
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Результат вычисления '2 + 2' = 4.0"
      }
    ]
  }
}
```

## Доступные инструменты (Tools)

### 1. echo

Простой инструмент для тестирования, возвращает введенный текст.

**Параметры:**
- `text` (string, required) - Текст для echo

**Пример использования:**
```json
{
  "name": "echo",
  "arguments": {
    "text": "Hello, World!"
  }
}
```

**Ответ:**
```
Echo: Hello, World!
```

---

### 2. add

Складывает два числа в формате "a + b".

**Параметры:**
- `text` (string, required) - Выражение в формате "число + число"

**Пример использования:**
```json
{
  "name": "add",
  "arguments": {
    "text": "5 + 3"
  }
}
```

**Ответ:**
```
Result: 8.0
```

**Ограничения:**
- Поддерживает только сложение двух чисел
- Формат: "a + b"

---

### 3. getCurrentTime

Возвращает текущее время в миллисекундах (Unix timestamp).

**Параметры:**
Нет параметров

**Пример использования:**
```json
{
  "name": "getCurrentTime",
  "arguments": {}
}
```

**Ответ:**
```
Current time: 1730851200000
```

---

### 4. getWeather

Получает информацию о погоде для указанного города.

**Параметры:**
- `city` (string, required) - Название города

**Пример использования:**
```json
{
  "name": "getWeather",
  "arguments": {
    "city": "Москва"
  }
}
```

**Ответ:**
```
Погода в городе Москва:
🌡️ Температура: -5°C
☁️ Условия: Облачно
💧 Влажность: 80%
🌬️ Скорость ветра: 15 км/ч
```

**Поддерживаемые города:**
- Москва / Moscow
- Санкт-Петербург / Saint Petersburg
- Казань / Kazan
- Новосибирск / Novosibirsk
- Екатеринбург / Yekaterinburg
- London
- Paris
- New York
- Tokyo

Для других городов генерируются случайные данные погоды.

**Примечание:** Это демо-реализация. В production окружении следует интегрировать реальный API погоды (OpenWeatherMap, WeatherAPI и т.д.).

---

### 5. calculate

Вычисляет математическое выражение с поддержкой базовых операций и скобок.

**Параметры:**
- `expression` (string, required) - Математическое выражение

**Поддерживаемые операции:**
- Сложение: `+`
- Вычитание: `-`
- Умножение: `*`
- Деление: `/`
- Скобки: `(` `)`
- Числа с плавающей точкой

**Примеры использования:**

1. Простое сложение:
```json
{
  "name": "calculate",
  "arguments": {
    "expression": "2 + 2"
  }
}
```
Ответ: `Результат вычисления '2 + 2' = 4.0`

2. Выражение со скобками:
```json
{
  "name": "calculate",
  "arguments": {
    "expression": "(10 - 5) * 3"
  }
}
```
Ответ: `Результат вычисления '(10 - 5) * 3' = 15.0`

3. Деление с плавающей точкой:
```json
{
  "name": "calculate",
  "arguments": {
    "expression": "15.5 / 2.5"
  }
}
```
Ответ: `Результат вычисления '15.5 / 2.5' = 6.2`

4. Сложное выражение:
```json
{
  "name": "calculate",
  "arguments": {
    "expression": "((10 + 5) * 2) / (3 + 2)"
  }
}
```
Ответ: `Результат вычисления '((10 + 5) * 2) / (3 + 2)' = 6.0`

**Алгоритм:**
Использует метод двух стеков (Two Stack Algorithm) для корректной обработки приоритета операций:
- Один стек для значений (numbers)
- Один стек для операторов (operators)
- Соблюдение приоритета операций (* и / выполняются перед + и -)
- Обработка скобок

**Обработка ошибок:**
- Деление на ноль: `ArithmeticException`
- Недопустимые символы: `IllegalArgumentException`
- Некорректный синтаксис: `IllegalArgumentException`

---

## Обработка ошибок

Сервер возвращает стандартные JSON-RPC ошибки:

### Метод не найден (Method not found)
```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "error": {
    "code": -32601,
    "message": "Method not found: unknown_method"
  }
}
```

### Внутренняя ошибка (Internal error)
```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "error": {
    "code": -32603,
    "message": "Internal error: <error details>"
  }
}
```

### Отсутствующие параметры
```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "error": {
    "code": -32603,
    "message": "Internal error: Missing 'expression' in arguments"
  }
}
```

## Интеграция с AI-агентами

### Использование в проекте

Агент взаимодействует с MCP сервером через `InteractYaGptWithMcpService`:

1. **Получение списка инструментов** - вызов `tools/list`
2. **Формирование промпта** - добавление информации о доступных tools в системный промпт
3. **Отправка в LLM** - YandexGPT получает список доступных инструментов
4. **Обработка tool calls** - когда LLM решает вызвать инструмент
5. **Выполнение через MCP** - вызов `tools/call` на MCP сервере
6. **Возврат результата** - результат передается обратно в LLM для финального ответа

### Пример flow взаимодействия

```
User: "Какая погода в Москве?"
  ↓
Agent → MCP: tools/list
  ↓
MCP → Agent: [echo, add, getCurrentTime, getWeather, calculate]
  ↓
Agent → YandexGPT: "У тебя есть инструмент getWeather..."
  ↓
YandexGPT → Agent: {tool_call: "getWeather", args: {city: "Москва"}}
  ↓
Agent → MCP: tools/call(getWeather, {city: "Москва"})
  ↓
MCP → Agent: "Погода в городе Москва: ..."
  ↓
Agent → YandexGPT: "Результат вызова инструмента: ..."
  ↓
YandexGPT → User: "В Москве сейчас -5°C, облачно..."
```

## Расширение функционала

### Добавление нового инструмента

1. Добавьте определение инструмента в список `mcpServerTools`:

```kotlin
McpServerTool(
    name = "myNewTool",
    description = "Описание инструмента",
    inputSchema = buildJsonObject {
        put("type", "object")
        putJsonObject("properties") {
            putJsonObject("param1") {
                put("type", "string")
                put("description", "Описание параметра")
            }
        }
        putJsonArray("required") {
            add("param1")
        }
    }
)
```

2. Добавьте обработчик в `handleToolsCall`:

```kotlin
"myNewTool" -> {
    val param1 = arguments["param1"]?.jsonPrimitive?.content
        ?: throw IllegalArgumentException("Missing 'param1' in arguments")
    myNewToolImplementation(param1)
}
```

3. Реализуйте логику инструмента:

```kotlin
private fun myNewToolImplementation(param1: String): String {
    // Ваша логика
    return "Результат работы инструмента"
}
```

### Интеграция с внешними API

Для интеграции с реальными API (например, OpenWeatherMap для погоды):

1. Добавьте зависимость Ktor Client в `build.gradle.kts`
2. Создайте HTTP клиент
3. Замените mock-данные реальными API вызовами

Пример:
```kotlin
private suspend fun getWeatherFromAPI(city: String): String {
    val client = HttpClient()
    val response: HttpResponse = client.get("https://api.openweathermap.org/data/2.5/weather") {
        parameter("q", city)
        parameter("appid", "YOUR_API_KEY")
    }
    // Обработка response
}
```

## Конфигурация

### Порт сервера

Изменить порт можно в `KtorServer.kt`:

```kotlin
embeddedServer(Netty, port = 8080) {
    // ...
}
```

### Timeout и другие настройки Ktor

См. официальную документацию Ktor: https://ktor.io/docs/

## Безопасность

**Важные замечания:**

1. **Калькулятор**: Текущая реализация безопасна, так как не использует `eval()` и обрабатывает только числа и базовые операции
2. **Аутентификация**: Текущая версия не содержит аутентификации. Для production следует добавить:
   - API ключи
   - JWT токены
   - Rate limiting
3. **Валидация входных данных**: Все параметры валидируются перед использованием
4. **CORS**: При необходимости настройте CORS в Ktor

## Тестирование

### Тестирование через curl

```bash
# Initialize
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":"1","method":"initialize","params":{}}'

# List tools
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":"2","method":"tools/list","params":{}}'

# Call calculator
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":"3","method":"tools/call","params":{"name":"calculate","arguments":{"expression":"2+2"}}}'

# Get weather
curl -X POST http://localhost:8080/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":"4","method":"tools/call","params":{"name":"getWeather","arguments":{"city":"Москва"}}}'
```

## Логирование

Сервер использует встроенное логирование Ktor. Для детального логирования:

```kotlin
install(CallLogging) {
    level = Level.INFO
}
```

## Производительность

- **Stateless**: Каждый запрос обрабатывается независимо
- **Lightweight**: Нет тяжелых зависимостей
- **Concurrent**: Ktor поддерживает параллельную обработку запросов

## Дальнейшее развитие

Возможные улучшения:

1. **Больше инструментов**:
   - Работа с файловой системой
   - Интеграция с базами данных
   - Web scraping
   - Генерация изображений

2. **Продвинутая обработка**:
   - Стриминг ответов
   - Асинхронные операции
   - Кэширование результатов

3. **Мониторинг**:
   - Метрики использования инструментов
   - Логирование ошибок
   - Health checks

4. **Безопасность**:
   - Rate limiting по IP
   - API ключи для аутентификации
   - Sandbox для выполнения кода

## Ссылки

- [Model Context Protocol Specification](https://spec.modelcontextprotocol.io/)
- [Ktor Documentation](https://ktor.io/docs/)
- [JSON-RPC 2.0 Specification](https://www.jsonrpc.org/specification)

## Версия

Текущая версия сервера: **1.0.0**

Дата последнего обновления: **2025-11-05**
