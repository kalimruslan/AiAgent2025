# MCP Server - Быстрый старт

## 🚀 Подключение к Claude Desktop

### Локальный сервер (за 3 шага)

### Шаг 1: Настройте API ключи

Создайте или обновите `local.properties` в корне проекта:

```properties
TRELLO_API_KEY=ваш_ключ
TRELLO_TOKEN=ваш_токен
OPENWEATHER_API_KEY=ваш_ключ_погоды
```

### Шаг 2: Проверьте работоспособность

```bash
# Из корня проекта
./gradlew :server:runStdio
```

Введите тестовую команду:
```json
{"jsonrpc":"2.0","id":"1","method":"initialize","params":{}}
```

Должен прийти JSON ответ с информацией о сервере. Нажмите `Ctrl+D` для выхода.

### Шаг 3: Настройте Claude Desktop

#### macOS
```bash
nano ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

#### Windows
```
notepad %APPDATA%\Claude\claude_desktop_config.json
```

#### Linux
```bash
nano ~/.config/Claude/claude_desktop_config.json
```

**Добавьте конфигурацию:**

```json
{
  "mcpServers": {
    "llm-agent": {
      "command": "/ПОЛНЫЙ/ПУТЬ/К/ПРОЕКТУ/server/run-mcp-server.sh",
      "env": {
        "TRELLO_API_KEY": "ваш_ключ",
        "TRELLO_TOKEN": "ваш_токен",
        "OPENWEATHER_API_KEY": "ваш_ключ"
      }
    }
  }
}
```

**⚠️ Важно:** Замените `/ПОЛНЫЙ/ПУТЬ/К/ПРОЕКТУ/` на реальный путь к проекту!

Например:
- macOS: `/Users/username/AndroidStudioProjects/kmp/LlmAgent2025/server/run-mcp-server.sh`
- Windows: используйте вариант с Gradle из `claude-desktop-config.json`

### Шаг 4: Перезапустите Claude Desktop

1. Полностью закройте Claude Desktop
2. Запустите заново
3. Откройте Developer Tools (⌘+Shift+I / Ctrl+Shift+I)
4. В консоли проверьте отсутствие ошибок

## ✅ Проверка работы

Попробуйте в чате с Claude:

```
Получи погоду в Токио
```

Или для Trello (замените YOUR_BOARD_ID на реальный ID):

```
Покажи статистику моей Trello доски (boardId: YOUR_BOARD_ID)
```

## 📋 Доступные инструменты

### Погода
- `getWeather` - погода для города

### Trello
**Базовые:**
- `trello_getCards` - список карточек
- `trello_createCard` - создать карточку
- `trello_getCard` - информация о карточке
- `trello_getSummary` - статистика доски

**Умные:**
- `trello_quickTask` - быстрая задача ("сегодня", "завтра")
- `trello_moveCard` - переместить карточку
- `trello_updateCard` - обновить карточку
- `trello_searchCards` - поиск карточек

**Массовые:**
- `trello_batchCreateCards` - создать много карточек
- `trello_bulkUpdate` - обновить много карточек
- `trello_bulkMove` - переместить много карточек

## 🔧 Отладка

**Проблема:** Сервер не подключается

**Решение:**
1. Проверьте путь в конфигурации Claude Desktop
2. Убедитесь, что скрипт исполняемый: `chmod +x server/run-mcp-server.sh`
3. Проверьте логи в Developer Tools Claude Desktop
4. Попробуйте запустить вручную: `./gradlew :server:runStdio`

**Проблема:** Trello инструменты не работают

**Решение:**
1. Проверьте API ключи в `local.properties`
2. Убедитесь, что ключи переданы в `env` секцию конфигурации Claude Desktop
3. Получите Trello ключи: https://trello.com/power-ups/admin

## 📚 Дополнительно

Полная документация: [README_CLAUDE_INTEGRATION.md](./README_CLAUDE_INTEGRATION.md)

## 🎯 Примеры использования

**Создать задачу на сегодня:**
```
Создай задачу "Написать отчет" на сегодня в Trello (boardId: abc123)
```

**Найти просроченные задачи:**
```
Найди все просроченные задачи на доске (boardId: abc123)
```

**Массово создать задачи:**
```
Создай 3 задачи в Trello: "Задача 1", "Задача 2", "Задача 3" в списке (listId: xyz789)
```

**Получить погоду:**
```
Какая погода в Сан-Франциско?
```

---

## 🌐 Удалённый сервер (VDS)

Если ваш MCP сервер работает на VDS (например, Beget), используйте один из способов:

### Способ 1: HTTP-to-Stdio прокси (Рекомендуется)

1. **Запустите HTTP сервер на VDS:**
```bash
# На VDS
./gradlew :server:run
# Или используйте systemd service
```

2. **Настройте URL в local.properties:**
```properties
REMOTE_MCP_SERVER_URL=https://kalimruslan-rt.ru
```

3. **Конфигурация Claude Desktop:**
```json
{
  "mcpServers": {
    "llm-agent-remote": {
      "command": "/путь/к/проекту/server/run-mcp-proxy.sh",
      "env": {
        "REMOTE_MCP_SERVER_URL": "https://kalimruslan-rt.ru"
      }
    }
  }
}
```

### Способ 2: SSH туннель

1. **Настройте SSH доступ без пароля:**
```bash
ssh-copy-id username@kalimruslan-rt.ru
```

2. **Отредактируйте `server/run-mcp-remote.sh`:**
```bash
SSH_HOST="kalimruslan-rt.ru"
SSH_USER="your_username"
PROJECT_PATH="/path/to/project"
```

3. **Конфигурация Claude Desktop:**
```json
{
  "mcpServers": {
    "llm-agent-ssh": {
      "command": "/путь/к/проекту/server/run-mcp-remote.sh"
    }
  }
}
```

**Подробная документация:** [README_REMOTE_SERVER.md](./README_REMOTE_SERVER.md)
